node_modules/
.env
.env.local
.DS_Store
data/leads.json
*.log

# base da plataforma (gerada pelo seed)
data/db.json

# Backup de leads: contém dado de cliente, não vai para o repositório.
backup-leads-*.json
